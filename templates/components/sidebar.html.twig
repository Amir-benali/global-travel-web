<aside class="w-64 h-full flex flex-col">
    <div class="p-6 flex justify-center">
        <img src="{{asset('images/logo.jpg')}}" alt="global travel Logo" class="h-36 w-36">
    </div>
    
    <nav class="flex-1 px-4 space-y-1">
        {% set currentRoute = app.request.get('_route') %}
        
        <!-- Dashboard -->
        <a href="{{ path('app_dashboard') }}" class="flex items-center px-4 py-3 rounded-lg 
            {% if currentRoute == 'app_dashboard' %}text-blue-600 bg-blue-50
            {% else %}text-gray-600 hover:bg-gray-100{% endif %}">
            <i class="ri-dashboard-line mr-3 text-lg"></i>
            <span>Dashboard</span>
        </a>
        
        <!-- Activities -->
        <a href="{{ path('app_activity') }}" class="flex items-center px-4 py-3 rounded-lg 
            {% if currentRoute starts with 'app_activity' %}text-blue-600 bg-blue-50
            {% else %}text-gray-600 hover:bg-gray-100{% endif %}">
            <i class="ri-line-chart-line mr-3 text-lg"></i>
            <span>Activities</span>
        </a>

        <!-- Flights -->
        <a href="{{ path('app_flight') }}" class="flex items-center px-4 py-3 rounded-lg 
            {% if currentRoute starts with 'app_flight' %}text-blue-600 bg-blue-50
            {% else %}text-gray-600 hover:bg-gray-100{% endif %}">
            <i class="ri-plane-line mr-3 text-lg"></i>
            <span>Flights</span>
        </a>

        <!-- Users -->
        <a href="{{ path('app_user') }}" class="flex items-center px-4 py-3 rounded-lg 
            {% if currentRoute starts with 'app_user' %}text-blue-600 bg-blue-50
            {% else %}text-gray-600 hover:bg-gray-100{% endif %}">
            <i class="ri-user-line mr-3 text-lg"></i>
            <span>Users</span>
        </a>
        
        <!-- Car -->
        <a href="{{ path('app_car') }}" class="flex items-center px-4 py-3 rounded-lg 
            {% if currentRoute starts with 'app_car' %}text-blue-600 bg-blue-50
            {% else %}text-gray-600 hover:bg-gray-100{% endif %}">
            <i class="ri-car-line mr-3 text-lg"></i>
            <span>Car</span>
        </a>

        <!-- Hotel -->
        <a href="{{ path('app_hotel') }}" class="flex items-center px-4 py-3 rounded-lg 
            {% if currentRoute starts with 'app_hotel' %}text-blue-600 bg-blue-50
            {% else %}text-gray-600 hover:bg-gray-100{% endif %}">
            <i class="ri-hotel-line mr-3 text-lg"></i>
            <span>Hotel</span>
        </a>
    </nav>
    
    <div class="p-4 border-t border-gray-200">
            <a href="{{ path('app_settings') }}" class="flex items-center px-4 py-3 rounded-lg 
            {% if currentRoute starts with 'app_settings' %}text-blue-600 bg-blue-50
            {% else %}text-gray-600 hover:bg-gray-100{% endif %}">
            <i class="ri-settings-line mr-3 text-lg"></i>
            <span>Settings</span>
        </a>
        <a href="{{ path('app_signin') }}" class="flex items-center text-gray-600 hover:text-blue-600 w-full px-4 py-2 rounded-lg transition-colors">
            <i class="ri-logout-box-line mr-3 text-lg"></i>
            <span>Logout</span>
        </a>
    </div>
</aside>